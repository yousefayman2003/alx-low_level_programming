#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A C program that prints alphabets
 *
 * Return: Always 0 if success else fail
*/

int main(void)
{
	char ch = 'z';

	while (ch >= 'a')
	{
		putchar(ch);
		c--;
	}
	putchar('\n');

	return (0);
}
